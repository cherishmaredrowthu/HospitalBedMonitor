spring.application.name=hospital-bed-monitor
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Sindhu@1303

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


# Allow file uploads if needed
spring.servlet.multipart.max-file-size=2MB

# Ensure the server port is dynamic (Render requirement)
server.port=${PORT:8080}